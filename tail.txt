  border-radius: 8px;
  color: var(--text-primary, #e0e0e0);
  font-family: var(--font-mono), monospace;
  box-shadow: 0 10px 40px rgba(0,0,0,0.5);
}

.focus-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24px;
}
.focus-header h2 {
  font-size: 1.2rem;
  font-weight: 500;
  margin: 0;
  letter-spacing: 1px;
}
.focus-header .close-btn {
  background: transparent;
  border: none;
  color: var(--text-secondary, #888);
  cursor: pointer;
  padding: 4px;
}
.focus-header .close-btn:hover { color: var(--text-primary); }

.focus-task-section {
  margin-bottom: 24px;
}
.task-prompt {
  color: var(--text-secondary, #888);
  margin-bottom: 8px;
  font-size: 0.9rem;
}
.focus-task-input {
  width: 100%;
  background: transparent;
  border: none;
  border-bottom: 1px solid var(--border, #333);
  color: var(--text-primary, #e0e0e0);
  font-family: inherit;
  font-size: 1rem;
  padding: 8px 0;
  outline: none;
}
.focus-task-input:focus {
  border-bottom-color: var(--accent, #3dd2cc);
}

.focus-duration-section {
  display: flex;
  align-items: center;
  gap: 16px;
  margin-bottom: 32px;
  font-size: 0.9rem;
}
.duration-label { color: var(--text-secondary, #888); }
.duration-presets { display: flex; gap: 8px; align-items: center; }
.duration-btn {
  background: transparent;
  border: none;
  color: var(--text-secondary, #888);
  font-family: inherit;
  cursor: pointer;
  padding: 4px 8px;
}
.duration-btn:hover { color: var(--text-primary); }
.duration-btn.active { color: var(--accent, #3dd2cc); font-weight: bold; }
.custom-duration-input { display: flex; align-items: center; gap: 4px; }
.custom-duration-input input {
  width: 50px; background: transparent; border: 1px dashed var(--border); color: inherit; font-family: inherit; padding: 2px 4px; text-align: center;
}
.custom-duration-input button { background: transparent; border: none; color: var(--accent); cursor: pointer; }

.focus-timer-display { text-align: center; margin-bottom: 32px; }
.focus-timer-display .time { font-size: 4rem; font-weight: 300; letter-spacing: 2px; }

.focus-controls { display: flex; justify-content: center; gap: 16px; margin-bottom: 24px; }
.focus-btn {
  background: transparent; border: 1px solid var(--border, #333); color: var(--text-primary);
  padding: 8px 16px; border-radius: 4px; font-family: inherit; cursor: pointer;
  display: flex; align-items: center; gap: 8px; transition: all 0.2s;
}
.focus-btn:hover:not(:disabled) { background: rgba(255,255,255,0.05); }
.focus-btn:disabled { opacity: 0.5; cursor: not-allowed; }
.focus-btn.primary { color: var(--accent, #3dd2cc); border-color: var(--accent, #3dd2cc); }

.focus-divider { height: 1px; background: var(--border, #333); width: 100%; margin: 24px 0; }

.focus-dashboard { padding: 8px 0; }
.dashboard-stats { display: flex; justify-content: space-between; margin-bottom: 24px; }
.dashboard-stats .stat-col { text-align: center; }
.dashboard-stats .stat-value { font-size: 1.2rem; font-weight: 500; }
.dashboard-stats .stat-sub { font-size: 0.8rem; color: var(--text-secondary); margin-bottom: 4px; }
.dashboard-stats .stat-title { font-size: 0.7rem; color: var(--text-secondary); text-transform: uppercase; letter-spacing: 1px; }

.weekly-bar-container { margin-bottom: 24px; }
.weekly-label { font-size: 0.8rem; color: var(--text-secondary); margin-bottom: 12px; }
.gh-sparkline.big { height: 40px; gap: 6px; align-items: flex-end; }
.gh-sparkline.big .gh-spark-bar { width: 12px; background: var(--text-secondary); }

.identity-line { text-align: center; font-style: italic; color: var(--text-secondary); font-size: 0.9rem; }

.section-toggle { width: 100%; background: transparent; border: none; color: var(--text-secondary); font-family: inherit; text-align: left; cursor: pointer; padding: 0; }
.section-toggle:hover { color: var(--text-primary); }

.focus-block-content { margin-top: 16px; }
.block-preset-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 12px; margin-bottom: 16px; }
.block-checkbox { display: flex; align-items: center; gap: 8px; font-size: 0.9rem; cursor: pointer; color: var(--text-secondary); }
.block-checkbox:hover { color: var(--text-primary); }

.custom-block-input { display: flex; gap: 8px; margin-bottom: 12px; }
.custom-block-input input { flex:1; background: transparent; border: 1px dashed var(--border); color: inherit; font-family: inherit; padding: 6px 8px; }
.custom-block-input button { background: transparent; border: 1px solid var(--border); color: var(--text-primary); cursor: pointer; padding: 0 8px; }

.custom-sites-list { display: flex; flex-wrap: wrap; gap: 8px; }
.custom-site-tag { display: flex; align-items: center; gap: 6px; font-size: 0.8rem; border: 1px solid var(--border); padding: 2px 8px; border-radius: 12px; }
.custom-site-tag button { background: transparent; border: none; color: var(--text-secondary); cursor: pointer; padding: 0; display: flex; }
.custom-site-tag button:hover { color: red; }

.session-log-section h3 { font-size: 0.9rem; font-weight: normal; color: var(--text-secondary); margin-bottom: 16px; margin-top: 0; }
.empty-log { font-size: 0.85rem; color: var(--text-secondary); font-style: italic; }
.session-list { display: flex; flex-direction: column; gap: 8px; }
.session-item { display: flex; align-items: center; font-size: 0.85rem; }
.session-time { color: var(--text-secondary); width: 50px; }
.session-task { flex: 1; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; padding-right: 16px; }
.session-dur { color: var(--accent); }

/* Summary Modal */
.focus-summary-modal {
  background: var(--bg-primary, #141414);
  border: 1px solid var(--accent, #3dd2cc);
  padding: 40px;
  border-radius: 8px;
  text-align: center;
  max-width: 400px;
  color: var(--text-primary);
  font-family: var(--font-mono);
}
.summary-icon { color: var(--accent); margin-bottom: 16px; }
.summary-details { margin: 24px 0; }
.summary-task { font-size: 1.2rem; margin-bottom: 8px; }
.summary-time { color: var(--text-secondary); }
.identity-text { font-style: italic; margin-bottom: 32px; color: var(--text-secondary); }
